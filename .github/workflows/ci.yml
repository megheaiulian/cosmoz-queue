name: CI

on:
  pull_request:
    branches:
      - master
      - main
  push:
    branches:
      - beta
      - master
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  build:
    uses: Neovici/cfg/.github/workflows/foundry.yml@master
    secrets: inherit
